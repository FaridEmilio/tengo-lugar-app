import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  content: {},

  cargando: {
    flex: 1,
    alignItems: "center",
    alignContent: "center",
  },

  text: {
    color: "#800080",
    textTransform: "uppercase",
  },
});
